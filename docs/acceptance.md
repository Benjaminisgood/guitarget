# Guitarget 本地验收记录

记录日期：2026-09-05 至 2026-09-06，Asia/Shanghai。验证环境：macOS 26.6.2（25G83）。目标交付为本地 `dist/Guitarget.app`，不涉及外部发布。

**最新扩展交付见第 9 节：2026-09-06 05:14:37 正式包新增和弦库、独立调音器、合奏与和弦练习，139 项 XCTest 通过。** 第 1–8 节保留原目标的历史验收、硬件失败与真琴反馈；本次未重新进行硬件采集验收。

2026-09-06 02:38:00.726 的完整测试为 **102 项 XCTest 全部通过**，其中原目标范围为 **82 项（GuitarCore 46 项、GuitarAudio 36 项）**，另外 20 项来自独立任务加入的曲库、歌词与入门谱。真实原生窗口操作、内置麦克风的 100 秒真琴引导及输入格式修复后的三次启停已有证据。用户对真琴音高体验给出了正面反馈；等待模式的单次拨弦误推进修复后，真琴复测确认“每次拨弦只推进一个”。

**当前系统声音采集仍未通过最终验收：最新 QA 与正式包均在启动时超时。** 早前两次并发参考声源隔离通过的记录保留为历史证据；最新修复已使 HAL 阻塞不再卡住主窗口，并实测超时后仍可录谱、播放、保存及正常退出，但没有恢复本次系统采集。独立音高真值下的频率精度、完整真琴跟练、物理输入延迟和外接声卡断开恢复也尚未完成验收。本文分别记录算法、原生交互、硬件与真琴证据，不以某项失败否定其他项目已观察到的结果。最新系统阻塞与原生复核见第 7 节，当前正式包见第 8 节，第 6 节保留 01:05 历史包信息。

## 1. 自动化与原生编辑状态

| 项目 | 本次证据 | 结果与范围 |
| --- | --- | --- |
| GuitarCore（原目标） | [最新完整测试日志](../artifacts/tests-audit-final.log)，2026-09-06 02:38:00.726 | 46 项通过：乐理 10、练习状态机 21、文件安全 7、曲谱 8 |
| GuitarAudio | 同一最新完整测试日志 | 36 项通过：音频算法 22、生命周期 5、系统采集会话 3、持续音／技巧 6；原目标合计 82 项，0 失败 |
| 独立任务新增测试 | 同一最新完整测试日志 | 本地曲库 12、歌词 3、入门谱 5，共 20 项通过；当前工程总计 102 项，0 失败 |
| 音频生命周期与异步采集专项 | [专项测试日志](../artifacts/audio-lifecycle-async-tests.log)，02:37:14.055 | 8 项通过，覆盖取消、迟到回调、启动超时和硬件快照等待；属于上述 102 项的子集 |
| 01:05 历史测试 | [当时的完整测试日志](../artifacts/tests-final.log)，01:04:58 | 原目标 64 项加独立任务 20 项，共 84 项通过；保留为历史证据 |
| 早期原生编辑状态 | [编辑状态自检](editor-self-check.json) | 10 项同步／异步检查记录 `passed: true`，调用真实 `ScoreEditorState`、Binding 和 `UndoManager`；保留为历史证据 |
| 01:05 历史应用自检 | [当时的正式包自检记录](../artifacts/production-self-check.json) | 2026-09-06 01:05:15 构建的正式包启动后导出；13 项原生检查与音频离线自检均为 `passed: true` |
| 最新正式包应用自检导出 | [正式包原生应用自检记录](../artifacts/production-audit-final/self-check.json) | 23 项原生检查与音频离线自检均为 `passed: true`；这 23 项不是 XCTest，不计入上述 102 项，也未测试硬件采集或真琴 |

Core 覆盖六根空弦与十二品八度、十二根音的六类音阶、关系大小调、五种显式指型、CAGED 大小和弦与七级三和弦；还覆盖附点、三连音、小节容量、双声部对齐、同弦冲突、保存重开、撤销重做、跨小节单音延音与“整小节低音叠加八个八分旋律音”。文件安全测试拒绝整数极值、非法节奏、非整数 ticks、未知版本及非有限数值，并检查失败读取后原文件字节保持不变。

Audio 覆盖已知频率、静音、噪声、失谐、强谐波的八度判断、六弦合成信号、技巧、释放与清空、重复起音、低 E 持续音、分析时间与起音时间分离，以及定位、循环、暂停、变速与预备拍。这里的“通过”是确定输入下的算法和调度结果；测试不使用真实吉他。

早期 10 项原生编辑状态检查另外覆盖连续文档 Binding 更新、两位品位 12、原生撤销重做历史的生命周期、部分和弦延音、静音后的联动指板，以及撤销后等待异步文档记账再重做仍恢复原值。01:05 历史包的 13 项记录还包含独立任务加入的歌词、曲库与原声音频检查；[00:44 旧包的 13 项自检](../artifacts/microphone-restart/production-self-check.json)亦保留。最新 23 项还检查底层文本撤销隔离、无效 BPM 草稿和密集谱定位，真实窗口复测见第 7 节。这些自检不能替代真实窗口中的焦点、菜单、权限操作或真琴复测。

## 2. 真实原生窗口操作

下列项目来自本次主任务对真实 `.app` 窗口的交互验证，属于操作记录；没有为未操作的项目补写“通过”。

| 操作 | 已观察到的结果 | 证据或界限 |
| --- | --- | --- |
| 五个侧栏入口 | 指板、音阶、CAGED、三和弦、曲谱均可进入 | 本次原生窗口操作记录 |
| 调性拼写与关系大小调 | F 大调显示 B♭，关系小调为 D 小调 | 原生窗口观察；另有十二调自动化校验 |
| 原生新建 | ⌘N 创建曲谱文档窗口 | 本次原生窗口操作记录 |
| 两位品位录入与原生保存 | 键盘输入并保存品位 12 | [保存的曲谱](../artifacts/Native-Keyboard-Fixed.guitarget) 中为第 4 弦、第 12 品，旋律声部，小节内 1920 ticks |
| 原生撤销与重做 | 工具栏、⌘Z／⇧⌘Z、原生“编辑”菜单的撤销／重做三条路径均观察到 **12 → 1 → 12** | [公开 OSLog](../artifacts/editor-undo-native.log) 记录三组原生 undo／redo 调用；[验证后保存的曲谱](../artifacts/QA-Undo-Redo-Verified.guitarget) 保留第 4 弦第 12 品 |
| 麦克风与真实吉他采集 | 已完成内置麦克风的 100 秒真琴引导，记录音高、电平和阶段时间 | 详见第 3.1 节；提示目标音没有独立真值，不能据此计算实际频率精度 |
| 麦克风拒绝授权后的编辑与播放 | 明确显示麦克风权限被拒绝后，⌘N、新曲谱键入 12、D4 单音试听、空格整谱播放及原生保存均成功；整谱游标观察到 **2796 ticks** | [拒绝权限后保存的曲谱](../artifacts/QA-Permission-Denied.guitarget) 中为第 4 弦第 12 品；验证在独立 QA 实例完成，正式 Guitarget 的麦克风授权未改动 |
| 系统声音界面 | `afplay` 播放 440 Hz 参考时曾显示约 440.1 Hz、界面整数音分为 0 | 精确结果见下一节真实采集记录；界面舍入值不作为精度测量 |

麦克风拒绝测试是在系统设置中仅关闭 **Guitarget QA** 的麦克风权限，正常退出后使用同一二进制重新打开，再点击开始采集，观察到明确拒绝提示。随后完成上表的键盘、试听、整谱播放和保存操作，因此这项证据覆盖“采集被拒绝不会阻断编辑及普通播放”。

系统音频录制的拒绝路径尚未验证：尝试切换 QA 的该项授权时，系统要求 Touch ID 或密码；认证被取消，授权仍保持开启。不能把尝试打开系统设置或取消认证视作成功拒绝授权。

钢弦试听和播放路径已经执行；当前输出设备测试确认渲染时钟推进。音色是否达到原声钢弦的主观质量要求、扬声器实际可听响度和长期无爆音仍需听者确认。

## 3. 已知参考音的真实系统采集历史

本节及第 7 节早前两次隔离实验记录曾经通过的真实采集；最新 QA 与正式包复测均启动超时，当前状态以第 7 节“系统采集阻塞与后台修复后的最终复测”为准。

[硬件与系统采集报告](../artifacts/audio-hardware-smoke.json) 的类型为 `native-audio-hardware-smoke`，起止时间为 2026-09-05 **23:38:52–23:39:18**，总结果 `passed: true`。测试实际创建 CoreAudio process tap，使用受测试程序管理的 `afplay` 进程播放 [440 Hz 长参考音](../artifacts/reference-a4-long.wav)。这是真实系统音频路径的参考信号测试，报告明确记载 `realGuitarTested: false`。

| 测试 | 实测结果 | 判定 |
| --- | --- | --- |
| 整机混音 | 56 个音高帧；频率中位数 **440.086052 Hz**，相对 440 Hz 中位偏差 **+0.3385 音分**；观察频率范围 440.084291–440.793792 Hz | 通过 |
| 指定 `afplay` 进程 | 55 个音高帧；频率中位数 **440.085817 Hz**，中位偏差 **+0.3376 音分**；观察频率范围 440.084291–441.265728 Hz | 通过 |
| 来源进程退出 | 被管理的参考播放进程退出后，指定进程采集停止，状态显示“来源 App 已退出” | 通过 |
| 排除自身声音 | 停止外部参考播放器后，Guitarget 自身 A4 试听时钟推进；系统采集仍运行，但音高帧数 **0**、最大 RMS **0** | 通过，限本次设备与安静外部环境 |
| 输出设备切换 | 切换到 MacBook Air 扬声器后时钟推进，恢复原输出选择后时钟仍推进，原选择恢复成功 | 通过；报告未进行物理可听性测量 |

指定进程一项证明了“选择该 `afplay` 来源后可以采集到它的参考音”。本次只有一个外部发声进程，未同时运行第二个带不同参考频率的干扰 App，因此不能据此宣称已实测“排除其他同时发声 App”的隔离效果。后续独立的双来源实验见第 7 节。

输出切换一项的证据是 `AudioUnitSetProperty` 返回成功、应用中所选设备值更新，以及切换前后渲染时钟持续推进。本次没有回读 AudioUnit 实际生效的设备 ID，也没有测量物理输出声路，因此判定范围限于配置请求成功及播放调度继续运行，不能等同于已测得声音确实从指定扬声器输出。

该次采集的主时钟为 **JBL PartyBox 110，44,100 Hz**。process tap 和输入流最初报告 48,000 Hz，而 aggregate 实际协商及采集的采样率为 44,100 Hz。实现已按实际主时钟采样率分析；这次 440 Hz 参考测量验证了该 48 kHz／44.1 kHz 差异的处理。不能仅根据创建 tap 时的流描述假设实际采样率。

整机混音的“分析窗中心到结果交付”平均为 **53.157 ms**、最大 **63.271 ms**；指定进程平均 **53.038 ms**、最大 **59.560 ms**。这些值仅描述本次采样窗中心到软件交付的间隔，**不是**琴弦拨动到屏幕显示的端到端延迟，也**不是**声卡回环延迟。单个稳态 440 Hz 参考音的结果不能换算成“真琴准确率”。

## 3.1. 真实吉他与内置麦克风

用户使用真实吉他，通过 **MacBook Air 内置麦克风、44,100 Hz 名义采样率**完成本次引导。实际采集时间为 2026-09-06 **00:20:25–00:22:05**，持续 **100.085 秒**；保存了 **375 个有效音高帧、952 个电平样本**。证据包括[采集会话](../artifacts/guitar-capture-20260906-002001-D459E2/session.json)、[原始特征与电平](../artifacts/guitar-capture-20260906-002001-D459E2/frames.json)、[逐阶段复核](../artifacts/guitar-capture-20260906-002001-D459E2/analysis.md)及[完整数值](../artifacts/guitar-capture-20260906-002001-D459E2/analysis.json)。

用户在这次实际使用后反馈：**“其实我觉得蛮好的，很准。”** 这是用户对本次真琴使用体验的明确主观评价，保留原话。下面的数值记录用于说明本次数据所支持的量化范围。

按采样窗中心重新划分阶段，与保存的阶段及评价标记均一致。排除换弦准备后，**322 帧**落在评价窗口；其余 53 帧不进入下表的评价分母。

| 六弦提示阶段 | 评价有效帧 | 符合目标 MIDI | 符合目标 ±25 音分 | 中位估计频率 |
| --- | ---: | ---: | ---: | ---: |
| 第 6 弦 E2 | 10 | 0% | 0% | 92.9207 Hz |
| 第 5 弦 A2 | 71 | 100% | 100% | 110.0007 Hz |
| 第 4 弦 D3 | 49 | 100% | 100% | 147.2934 Hz |
| 第 3 弦 G3 | 44 | 93.2% | 90.9% | 195.2429 Hz |
| 第 2 弦 B3 | 53 | 94.3% | 94.3% | 246.0094 Hz |
| 第 1 弦 E4 | 48 | 93.8% | 93.8% | 329.5440 Hz |

这些是**有效音高帧相对提示目标的符合比例**。静音和没有可信音高的窗口不会产生 pitchFrame，未计入百分比分母；提示的 expectedMIDI 也不是对实际弹奏频率的独立测量。因此，100% 不能解释为整段无漏检、每次拨弦都成功，或已知真实频率下的 100% 准确率。原始 cents 相对算法最近的 MIDI 音符；分析中的“目标音分”另按提示目标重新计算。

低 E 记录存在待解释的异常。首段 10 帧全部为 MIDI 42（F♯2），约比 E2 提示高 208 音分，而且仅集中在评价窗口第 4.50–5.50 秒。后续低 E 保持段 13 帧中仅 1 帧符合 E2，重复段 34 帧中仅 2 帧符合 E2，符合比例分别为 7.7% 和 5.9%；两段主要候选为 MIDI 64、约 329.9 Hz，并在低电平尾部出现周期倍数相关候选。

**本次没有保存原始 PCM 波形，也没有独立音高真值或实际弦位标注。** 现有特征不足以区分真实输入内容、谐波／周期候选歧义与检测误差，不能把低 E 异常归因于演奏者或算法。用户的主观反馈与这些待解释数据分别保留。引导中的起音标签也没有独立拨弦时刻作核对，不能当作真实拨弦次数或跟练判定已经通过。

本次各评价阶段的**采样窗中心到软件交付平均间隔约 61–63 ms**，阶段最大值约 68–72 ms；相邻有效帧的中位间隔约 100 ms，是另一项指标。这些均来自软件时间戳，**不是实际拨弦到显示的物理端到端延迟**。物理输入延迟仍需声卡回环或外部时间基准测量。本次运行的是采集引导，当时尚未运行真琴跟练；后续逐次拨弦和按节拍操作结果见第 4 节。

## 3.2. 麦克风重新启动与输入格式修复

真琴引导后的重新启动暴露了输入格式协商问题。[修复前日志](../artifacts/microphone-restart-before.log) 多次记录硬件输入为 **1 通道、44,100 Hz**，客户端仍为 **1 通道、48,000 Hz**，随后出现 `Failed to create tap, config change pending!`；配置通知约在 100 ms 后到达。中间版本仅等待客户端格式自行匹配，三次均以“声音输入格式协商超时”结束，[失败报告](../artifacts/microphone-restart/microphone-restart-smoke.json) 保留 `passed: false`。这些失败不能因最终修复通过而从验收历史中删除。

输入格式窄修读取所选设备的实时硬件输入格式并核对设备 ID 与标称采样率，就绪后以该硬件格式安装 tap，主动配置客户端输出总线。配置通知用于硬件尚未就绪时重新检查，同时保留有界超时和取消机制；不再等待客户端自行变为 44.1 kHz。[麦克风启停报告](../artifacts/microphone-restart-final/microphone-restart-smoke.json) 来自 00:48 构建的正式应用，时间为 2026-09-06 **00:49:06–00:49:29**，三次启动后各观察约 7 秒，再停止并重新开始；总结果及三次结果均为 `passed: true`。

| 次数 | 硬件／客户端格式 | 新收到的采样数 | 观察结果 |
| --- | --- | ---: | --- |
| 1 | 均为 44,100 Hz、1 通道 | 313,110 | 引擎运行，采集已启动 |
| 2 | 均为 44,100 Hz、1 通道 | 308,700 | 停止后重新启动成功 |
| 3 | 均为 44,100 Hz、1 通道 | 308,700 | 再次停止后重新启动成功 |

这项测试确认本机内置麦克风在该次输入格式修复包中三次启停均持续收到真实音频采样。报告明确记载 `realGuitarAccuracyMeasured: false`，没有测试真琴识别准确性、完整跟练或物理输入延迟；也没有重新执行系统声音的完整硬件验收。后续起音算法修复包的独立证据见第 4、6 节。

## 4. 离线六弦与运行预算

### 按节拍跟练的本次窗口结果

用户选择暂用 **MacBook Air 内置扬声器**听节拍，输入仍为内置麦克风。切换到“按节拍跟练”、旋律声部，以同一 80 BPM 的八个 E2 空弦目标运行；主任务在正式窗口读到 **8/8 正确、漏音 0、平均起音偏差 35 ms**。逐音结果显示“拖拍 26 ms、拍点准确、抢拍 60 ms、抢拍 21 ms、抢拍 59 ms、抢拍 38 ms、拍点准确、抢拍 51 ms”。较早一次窗口还显示过八个漏音，但该次没有确认用户是否已开始演奏，因此不用于评价识别质量。

这确认按节拍模式可以运行到结束并显示逐音及汇总结果。该轮演奏时“输入延迟”为 **0 ms**；之后用户运行跟拍校准并反馈“已显示估计偏移”，主任务读取到 **“已用 8 次起音估计偏移：+4 ms（含人为跟拍偏差）”**，输入延迟字段同步变为 **4 ms**。校准流程及字段应用在本次操作中通过；8/8、35 ms 是校准之前的那轮结果，不能冒充使用 +4 ms 后重新跑过的结果。

35 ms 是界面计算的平均绝对节拍偏差，+4 ms 是含人为跟拍偏差的校准估计，两者都不是物理输入延迟。该轮未佩戴耳机、未保存原始声学记录或独立标注拨弦时刻，扬声器节拍串音的影响未单独排除，因此不能把窗口的 100% 扩大为独立测得的起音准确率。抢拍、拖拍、错音和漏音的受控真琴实验仍有待完成。

### 真琴等待模式的误推进与逐次拨弦复测

内置麦克风启停恢复后，打开 [8 个重复 E2 验收谱](../artifacts/Real-Guitar-Practice.guitarget)，用户按提示只拨一次最粗第六弦空弦并保持，反馈 **“只拨一下却连续推进”**。原生跟练窗口同时显示“练习完成 · 8/8 正确”。这属于误通过，不能把界面的 100% 计入成功验收。

独立[复现脚本](../artifacts/onset-probe/main.swift)用单次起音后持续三秒的 E2 周期信号，叠加 `sin(nωt)/n` 的 12 个谐波，旧起音检测器在 44.1 kHz 下报出 **31 个起音**；恒幅与指数衰减均可复现。[旧结果](../artifacts/onset-probe/harmonics-before.log)保留全部时间戳。纯正弦没有复现，因此仅测纯正弦或本项目特定合成器不足以验证真实拨弦的起音可靠性。该合成复现证明起音检测存在周期波形误触发，但没有原始真琴 PCM，不能证明两者波形完全相同。

起音算法改为逐采样的连续功率平滑，按秒计算能量及亮度背景；保持原有 90 ms 起音间隔下限，YIN 音高算法未改。独立脚本的 96 个组合（44.1/48 kHz、E2/A2/D3/E4、128 样本/100 ms 分块、1/4/12 个谐波、恒幅/衰减）在[修复后](../artifacts/onset-probe/harmonics-after.log)均只有一个起音。推进端同时要求每次新起音之后重新累计 120 ms 稳定音高，并允许正常 100 ms 回调的轻微时刻抖动，200 ms 缺窗仍重置。

01:05 新版正式应用启动后，用户用同一真实吉他与内置麦克风复测：一次拨动最粗第六弦 E2 空弦并保持约 3 秒，再重新拨弦。用户明确反馈：**“正常了，每次拨弦只推进一个”。** 主任务在原生窗口中观察到已通过 5 个目标、停在第 6 个目标，状态为“请重新拨弦后继续”，没有单次拨弦后一口气完成 8/8。该操作复测通过的范围是本次低 E2 重复音的持续音不连跳、重新拨弦逐次推进；不表示已测试 ±25 音分边界、跨小节延音、所有拨弦力度或独立声学真值。按节拍模式的 8/8 运行及八次跟拍校准已另行完成，受控抢拍／拖拍／错音／漏音及 ±150 ms 边界仍待验证，详见本节上文。

[优化后离线诊断](../artifacts/audio-optimized/audio-diagnostics.json) 使用本项目合成器生成六根标准空弦，再用 YIN 分析。六个结果均通过，最大绝对音分偏差约 **0.3054 音分**。分析窗为 4096 个 48 kHz 原始样本，即 **85.333 ms**；这是窗长度，不是函数耗时。

该记录的单窗处理耗时约 **0.315–0.320 ms**。六弦同时渲染、每块 256 样本时，平均块耗时 **0.0164 ms**、最大 **0.0604 ms**，相应 48 kHz 块时限为 **5.333 ms**；`processingBudgetPassed: true`。机器负载和编译配置会影响耗时，不应把离线预算结果推广为所有设备无丢帧保证。

[未优化诊断](../artifacts/audio-unoptimized/audio-diagnostics.json) 保留了早期不满足分析预算的结果：单窗约 63–65 ms，`processingBudgetPassed: false`。当前工程对 Debug 下的 GuitarAudio 目标启用优化，不能用未优化的旧报告代表当前配置，也不能把旧失败记录删除后当作从未发生。

这些离线诊断明确标记 `hardwareCaptureTested: false`、`realGuitarTested: false`。真实系统采集的证据来自上一节的独立报告。

## 5. 尚未完成的实际验收

| 项目 | 当前状态 | 完成时需要记录 |
| --- | --- | --- |
| 当前正式包系统声音采集 | **未通过：02:42:40–02:42:51 的正式包复测启动超时**；后台修复后的 QA 同样失败。超时后编辑、播放及原生保存已验证可用；早前两次隔离通过保留为历史 | 恢复真实音频回调后重新执行整机混音、指定来源隔离、自身排除、来源退出及输出切换；保留失败与恢复证据 |
| 系统音频录制拒绝授权 | 尚未验证；切换需 Touch ID／密码，取消认证后授权仍开启 | 实际拒绝后的采集状态、错误提示及编辑／试听行为 |
| 外接声卡、输入通道及物理断开 | 尚未验证 | 设备、采样率、输入通道、切换及实际拔出后的行为 |
| 真琴频率精度与识别覆盖率 | 已完成内置麦克风真琴采集及用户体验反馈；独立真值下的定量精度尚未完成 | 独立参考音高、原始 PCM、弱／强拨、低 E、失谐点，以及包含无音高窗口的逐次记录 |
| 真琴“弹对再前进” | 低 E2 重复音逐次推进已通过真琴复测，用户确认每次拨弦只推进一个；完整验收尚未完成 | ±25 音分边界、120 ms 稳定边界、跨小节延音不重新拨弦、手动跳过及不同拨弦力度 |
| 真琴按节拍跟练 | 本次八个 E2 的运行、结果显示及八次跟拍校准已验证；受控评分边界仍未完成 | 其他声部、±150 ms 边界、主动抢拍／拖拍／漏音及排除扬声器串音后的结果 |
| 物理端到端输入延迟 | 尚未验证 | 声卡回环或有外部时间参考的测量方法、原始记录及统计；跟拍校准含人为误差 |
| 合成音色与声学串音 | 真琴识别体验已有用户正面反馈；合成音色与串音未作专项验收 | 钢弦听感、音量、长时间播放爆音／丢音、耳机与扬声器条件 |

跟练默认关闭目标声部和另一声部；同声部和弦、死音与连续变音技巧只作明确跳过，不实施复音评分。麦克风与扬声器同用时不承诺消除串音，应在真实演奏验收中注明是否佩戴耳机。

可复现入口为 `./script/test.sh`、`./script/build_and_run.sh --build-only` 和 `./script/stage_qa.sh --hardware-smoke`。硬件 smoke 会启动真实采集和自身管理的参考播放器；离线自检与单元测试不会把音高算法通过自动转换成真实演奏验收通过。

## 6. 01:05 历史交付包

正式包位置为 [dist/Guitarget.app](../dist/Guitarget.app)。这一历史构建并临时签名完成于 2026-09-06 **01:05:15 +0800**，实际启动并导出[当时的应用自检](../artifacts/production-self-check.json)：13 项原生检查与离线音频自检均通过。五入口学习室及原生编辑窗口的交互证据来自此前验证；第 3.2 节三次麦克风启停使用的是 00:48 输入格式修复包。01:05 包完成了第 4 节低 E2 重复音逐次推进的真琴复测，完整真琴跟练验收范围仍见第 5 节。

二进制 `Contents/MacOS/Guitarget` 的 SHA-256：

```text
c967cf16633059eb8fedd73a93f678d2f48911b0f593765d5e95fafb8ea68038
```

[最终起音修复构建日志](../artifacts/build-onset-final.log)、[签名与链接库复核](../artifacts/bundle-verification.log)及[源码文件 SHA-256 清单](../artifacts/source-sha256.txt)保留在本地。`codesign --verify --deep --strict` 通过；链接项均来自 Apple 系统框架和系统 Swift 库，应用包内没有 WebView 资源、Python、外部运行时或第三方动态库。本次未对外发布或公证。此前的[首次构建日志](../artifacts/build-final.log)及[输入格式修复构建日志](../artifacts/build-microphone-restart-final.log)作为历史记录保留。

麦克风拒绝授权的历史验证使用独立的 `Guitarget QA`，该项测试没有修改正式 Guitarget 的麦克风授权。QA 也被其他任务用于验收，此处不声明其当前运行或授权状态。常规应用启动不自动采集。

## 7. 01:40 后完成度复核

[完整测试日志](../artifacts/tests-audit-final.log)记录最近一次重跑于 02:38:00.726 完成，102 项 XCTest 全通过。新增乐理覆盖所有十二根音、六类音阶、五种显式指型及界面共用的练习谱生成路径；修复布鲁斯盒形边界扩展音、调性音名的悬停／辅助功能拼写，以及全指板 15／24 品切换。指型名称按关系大调的 CAGED 位置约定显示；真实手指可弹性仍是人工验收项。

音频新增五项生命周期测试，包含真正排队后跨停止／重启交付的旧音高和静音帧、同步评分回调停采、运行中配置变化与迟到配置通知。采集配置观察器现在保留至采集停止，设备／格式变化会明确停止并提示；物理设备的运行中采样率切换尚未实测。系统录音权限撤回后是否交付静音缓冲仍需真实撤权验证，不能仅用静音检测代替权限状态证据。

另有六项持续音／技巧测试：普通长音和击弦／勾弦继承的前音均重建已衰减的延迟线；循环在构造时准备快照，回调仅复制固定大小状态。重新定位允许琴体／混响旧尾音清空，并用 4 ms 淡入抑制切入不连续。测试比较了实际波形，而非只检查触发计数；尚未进行持续音恢复的专项真人听评。三十二分的半音／全音推弦、滑音、击弦和下降勾弦随实际事件时长缩短过渡，覆盖 0.5×、1×、3×。预备拍期间公开谱面位置固定在请求起点，仍保留负向音频时钟用于跟练锚点，界面不高亮尚未发声的音符。

### 两个并发进程的真实系统采集历史

01:41 的[系统隔离报告](../artifacts/system-isolation/system-isolation-smoke.json)来自独立 QA 应用。`afplay` 播放 440 Hz，独立 Swift／AVAudioPlayer 进程播放 697 Hz；选择其中一方时两进程均保持运行。两者 `bundleID` 均为空，因此这次具体覆盖按 CoreAudio 进程对象隔离，尚不能据此推广为所有多进程 App 的 bundleID 聚合行为。

| 轮次与选择来源 | 有效音高帧 | 全部位于参考 ±5 音分 | 中位频率 | 中位偏差 |
| --- | ---: | ---: | ---: | ---: |
| 首次：440 Hz 进程 | 433 | 是 | 440.0267 Hz | +0.105 音分 |
| 首次：697 Hz 进程 | 61 | 是 | 697.1398 Hz | +0.347 音分 |
| 第二次：440 Hz 进程 | 62 | 是 | 440.0270 Hz | +0.106 音分 |
| 第二次：697 Hz App | 63 | 是 | 697.1402 Hz | +0.348 音分 |

停止外部播放器后，整机 tap 持续收到回调；QA 自身 A4 试听期间输出时钟推进，采集 RMS 最大值为 0、有效音高帧为 0，自身声音排除通过。显式选择 MacBook Air 扬声器，请求与 AudioUnit 实际设备 ID 均为 95。选择无效设备 ID 后状态为暂停、`isPlaying=false`；恢复有效设备再继续，时钟重新推进。报告总结果为 `passed: true`。这些是数字音频路径的真实系统测试，没有测量物理扬声器声音或真琴准确性。

02:02:55–02:03:15 的[第二次隔离报告](../artifacts/system-isolation-bundled/system-isolation-smoke.json)总结果亦为 `passed: true`。此次 697 Hz 来源具有实际 `bundleID: com.guitarget.audio-test-source`，440 Hz 来源的 bundleID 仍为空；选择任一来源时两者均保持运行，上表两组帧全部落在各自参考 ±5 音分内。自身排除再次得到 0 个有效音高帧、最大 RMS 0，输出设备回读及失败后恢复也通过。这补充了本机带 bundleID App 的实际隔离证据，但未测试同一 bundleID 的多个子进程聚合；报告仍明确 `physicalOutputMeasured: false`、`realGuitarTested: false`。

复现入口：`./script/stage_qa.sh --system-isolation-smoke`，使用纯 Swift helper `script/AudioTestSource.swift`。需先正常退出 QA；只停止本次创建的两个播放器。

### 系统采集阻塞与后台修复后的最终复测

02:17:21 启动的旧同步 QA 在系统采集启动期间失去原生界面响应，未生成完成报告。02:18:14 的[进程采样](../artifacts/system-isolation-named-final/sample.txt)显示主线程停在 `AudioDeviceCreateIOProcIDWithBlock`；[中断观察记录](../artifacts/system-isolation-named-final/interrupted-report.json)明确标为 `passed: false`、`completionReportProduced: false`。应用在主线程同步等待 HAL 是窗口被阻塞的直接路径，但采样没有查明 HAL 服务不返回的具体原因，也不能确定当时运行到了隔离循环的哪一轮。界面访问超时后，仅向本任务创建且没有打开曲谱文档的 smoke 实例发送 SIGTERM；没有使用 SIGKILL 或重启 CoreAudio 服务。

系统 tap 的创建、检查与销毁已移到串行后台队列，启动等待设为 10 秒；取消后即时停止界面采集状态，HAL 返回后继续清理资源。每次采集使用独立缓冲区，迟到回调核对会话身份，设备枚举也改为后台单次读取及有界等待。[8 项音频专项测试](../artifacts/audio-lifecycle-async-tests.log)全部通过，其中新增三项模拟 HAL 阻塞、取消后独立缓冲区重试、启动超时与硬件快照超时。这些是受控生命周期测试，不能证明真实 HAL 已恢复。

| 最终复测 | 本地时间 | 实际结果 |
| --- | --- | --- |
| 后台修复后的真实 QA | 02:38:44–02:38:55 | [报告](../artifacts/system-isolation-async-final/system-isolation-smoke.json)为 `passed: false`，10 秒启动超时并取消 |
| 最新正式 `dist/Guitarget.app` | 02:42:40–02:42:51 | [报告](../artifacts/production-system-isolation-final/system-isolation-smoke.json)同样为 `passed: false`，系统采集未就绪 |

QA 的[修复后进程采样](../artifacts/system-isolation-async-final/sample.txt)显示主线程处于正常 AppKit 事件循环，后台硬件队列仍在同一 HAL 调用等待。[运行日志](../artifacts/system-isolation-async-final/runtime.log)约每 30 秒出现 IPC 错误 `0x10004003`（`MACH_RCV_TIMED_OUT`）。这支持“主线程阻塞已解除、真实系统采集仍受 HAL 调用超时阻塞”的结论；不能据此泛化为权限拒绝，也不宣称已查明 HAL 内部超时原因。

QA 超时后，主任务在同一真实窗口新建曲谱、键入 **12 品**、播放并观察到滑块推进至 **573 ticks**，随后通过原生保存得到 [Audit-System-Timeout-Editable.guitarget](../artifacts/Audit-System-Timeout-Editable.guitarget)，再用 ⌘Q 正常退出。保存文件已核对为第 1 弦第 12 品、四分音符、80 BPM。该操作验证超时后编辑、播放调度、保存与退出仍可用；不等同于系统音频采集成功，也没有测量物理扬声器输出。

### 原生文档复核

01:46–01:49 的 QA 原生窗口完成：打开有效密集谱 → 原生另存为 `Audit-Native-Save.guitarget` → 将首音 12 品改为 13 品 → 撤销回 12 品 → 关闭，无保存阻塞；关闭后 JSON 语义与原文件完全相同。新建文档直接按 `1`、`2` 得到 12 品；关闭出现原生保留／保存面板，点击取消后文档与音符仍在。

密集谱实际窗口确认品位数字、符尾和 PM 标记不再彼此覆盖；首次打开的横向起点及属性同步问题随后补修。重新打开 [Audit-Dense-Valid.guitarget](../artifacts/Audit-Dense-Valid.guitarget) 的原生截图确认：首音为 1 弦 12 品、三十二分音符、PM，左端首歌词可见；连续按 31 次右方向键到达首小节 3720 ticks、17 品时，谱面自动横滚且末歌词完整可见。再按一次右方向键进入第二小节，开启预备拍后滑块固定在 3840 ticks，未高亮尚未发声的音符。

文本字段额外复核曾发现标题在字段内撤销后，返回 Canvas 再撤销会错误恢复刚撤销的标题；[首次复现文档](../artifacts/Audit-Text-Undo-Before.guitarget)保留。中间修复版本又在原生重做时出现 `NSRangeException`，随后撤销管理器处于无效状态；[异常日志](../artifacts/text-redo-before.log)与[当时文档](../artifacts/Audit-Text-Undo-Redo-Before.guitarget)均保留。原因是 `NSTextStorage` 仍取得曲谱撤销管理器，将底层字符替换逆操作混入曲谱历史。最新修复向底层文本存储提供 `nil` 撤销管理器，字段菜单及快捷键显式调用曲谱历史；仅关闭文本视图的 `allowsUndo` 不足以隔离这条路径。

修复后的真实新建文档依次完成：键入 **12 品 → 标题“原生标题验收” → 字段撤销还原标题 → Canvas 撤销回 1 品 → 两次重做恢复 12 品及新标题**。BPM 的 `999` 草稿首次撤销、`-` 草稿首次重做均只恢复合法显示 **80**，没有撤销或重做音符。[最终保存文档](../artifacts/Audit-Text-Undo-Final.guitarget)已核对标题为“原生标题验收”、BPM 为 80、首音为 1 弦 12 品。[复测日志](../artifacts/text-undo-redo-final.log)按 **PID 12484** 筛选后未见上述异常；文件也保留 PID 11302 的旧失败条目，不能将两次运行混作同一结果。以上原生交互通过，历史失败不因修复成功而删除。

音阶窗口的 F 大调实际显示 F、G、A、B♭、C、D、E；第 1 弦第 6 品的辅助功能与悬停均为 B♭4。全指板开关关闭时读到空弦至 15 品，开启后存在至 24 品的实际指板按钮，宽度超过窗口时可横向滚动。

## 8. 02:38 原目标正式交付包（历史）

当前正式包为 [dist/Guitarget.app](../dist/Guitarget.app)，包含本轮原生编辑与异步系统采集修复。`Contents/MacOS/Guitarget` 的 SHA-256 已与实际文件核对：

```text
b520fa72f3862a5c7336f472688b7adfa63113489531d677b9ae2aa988edd8fe
```

[正式包自检](../artifacts/production-audit-final/self-check.json)记录 **23 项原生检查及音频离线自检均通过**，并明确 `hardwareCaptureTested: false`、`realGuitarTested: false`。[签名与链接库记录](../artifacts/bundle-verification-audit.log)记录本地 ad hoc 签名及 Apple 系统框架／Swift 库依赖；[源码 SHA-256 清单](../artifacts/source-sha256-audit.txt)保留本轮构建对应的源码信息。本次仅交付本地应用，没有对外发布。

当前正式包的真实系统声音复测仍以第 7 节的超时失败为准。正式包离线自检通过不替代硬件采集恢复，也不替代第 5 节尚未完成的真琴与外接设备验收。

### 最新麦克风复测与窗口交接

02:43:30–02:46:51 的[正式包麦克风启停报告](../artifacts/production-microphone-final/microphone-restart-smoke.json)为 `passed: false`：三次均停在“请求麦克风权限…”，新增样本均为 0。该记录未进入有效采样，不能说明新版麦克风启停已经通过，也不能作为音高算法准确性的测试。此前 00:48 包三次启停通过的记录保留在第 3.2 节，不能替代这次新包的实际验证。

主任务已请求用户处理 macOS 麦克风授权弹窗。原生控制工具明确拒绝操作 `com.apple.UserNotificationCenter`，因此未代点、未绕过系统授权界面；待用户完成授权后需重新执行三次启停检查。

最新正式应用已打开 [Real-Guitar-Practice.guitarget](../artifacts/Real-Guitar-Practice.guitarget) 并恢复“曲谱跟练”弹窗，选中“按节拍跟练”、旋律声部，输入延迟字段恢复为先前八次跟拍校准得到的 **4 ms**。当前采集未启用，“开始练习”禁用，未声称使用此偏移完成新一轮演奏。该字段为当前弹窗状态，不保证关闭应用后保存。

## 9. 05:14 学习室扩展交付

新目标增加四个栏目，保留原有五个栏目与曲库／歌词功能。正式包于 **2026-09-06 05:14:37 +0800** 构建并本地签名，已实际启动；路径仍为 [dist/Guitarget.app](../dist/Guitarget.app)。可操作说明见[新增学习栏目指南](learning-expansion.md)。

二进制 SHA-256：

```text
5ef369082c403271b7fd8cd05068073d482463de1a62eae448f2f76c25ef384d
```

### 自动化证据

[最终完整测试](../artifacts/tests-learning-expansion-final.log)于 **05:08:57.258** 结束，**139 项 XCTest，0 失败**。其中此前 102 项全部保留；新增 37 项为和弦库 10、调音器核心 11、调音参考音 3、合奏播放选项 2、伴奏编配 4、和弦练习状态机 7。

- 和弦库用独立有限范围暴力枚举验证结果集合，检查全部构成音、转位、合法连续横按、最多四指、取消和边界；常用排序不删候选。
- 调音器检查七种调弦、A4 重算、过期／重复／未来帧、稳定性、错八度与自动目标范围。非 440 Hz 参考音经离线渲染和音高检测验证；默认 440 路径保持一致。
- 四种伴奏覆盖十二根音、完整和弦音、每个实际同时发声时刻的整体四指及品差检查、双声部弦位隔离、文档保存重读、推荐音随小节和调性拼写。
- 和弦练习检查看／听／记忆阶段、单音新起音、提示与手动跳过、音频中断、采集关闭与恢复；自评不计入自动通过。

[最终正式包自检](../artifacts/learning-expansion-final/production-self-check.json)与[同源码 QA 自检](../artifacts/learning-expansion-final/qa-self-check.json)均为 `passed: true`，各含 **23 项原生状态检查及音频离线自检**。这 23 项属于原生编辑／曲库等自检，不是新增页面的自动 UI 测试，也不计入 139 项 XCTest。签名通过 `codesign --verify --deep --strict`。

首次 137 项测试虽通过，但复核发现排序首项使用稀疏转位和弦，部分伴奏上下弦组合需要五指；随后修正排序和全握型选择并增加整体指法检查。专项测试还纠正了 G 首选的预期：按“少用指”优先时，合法五弦 `32000x` 先于六弦 `320003`，后者仍保留在完整集合中。最终结果以 139 项完整通过为准。

### 真实原生窗口操作

| 项目 | 实际观察 |
| --- | --- |
| 九个栏目 | 最终正式应用显示原有五项及和弦库、调音器、合奏、和弦练习 |
| 和弦库 | 默认 C 显示 470 种、40 页；最终首项为常用 `x32010`。点击下一页变为 2/40，当前按法同步更新；根音低音筛选后 144 种，C9 对应 38 种并显示 C/E/G/B♭/D。扫弦进入播放状态 |
| 调音器 | Drop D 后第六弦显示 D2；A4 调至 440.5 Hz、锁定六弦，目标为 73.50 Hz；参考音播放时读数暂停。没有输入时显示空读数，不虚构检测结果 |
| 合奏播放 | 从第三小节 Am 开始时预备拍期间保持 Am；播放后循环推进到其他小节；暂停显示第二小节 G。选择 G4 并试听后恢复“继续伴奏”的原暂停状态；切换栏目停止本模块播放 |
| 伴奏原生文档 | 最终 QA 从“打开伴奏曲谱”新建并原生保存 [QA-Learning-Jam.guitarget](../artifacts/learning-expansion-final/QA-Learning-Jam.guitarget)。回读为 C 民谣分解、80 BPM、四小节、melody/bass 两声部；编辑器播放时滑块观察到 1747 ticks |
| 看图练习 | 无采集仍能开始一轮，显示 Em 与指法；手动继续一次再自评“还需练习”，结果为自动 0/4、手动 1，未误记自动通过 |
| 限时记忆 | 初始显示 Gm 指法与五秒倒计时，结束后图示和目标音名隐藏，仅提示演奏弦序，并提供看图提示 |
| 听示范后演奏 | 自动播放分解示范并明确暂停判定；示范结束后进入隐藏图示的逐弦演奏阶段 |

上述操作证明界面与播放控制路径可用，不是听者对音色的评价，也不是新模块的真琴准确率。原声参考频率的证据来自离线已知信号测试，未做物理扬声器频率测量。

### 本次未完成的硬件与真人部分

本次未重新启用麦克风、声卡或系统声音，也未改动系统权限。第 8 节麦克风待授权和第 7 节系统 HAL 超时没有因新增页面被宣布恢复。新调音器的真实读数、和弦逐弦练习的真琴表现，以及带伴奏串音环境下的结果均须另行实际验证。历史跟拍估计偏移仍为 **+4 ms**，含人为跟拍误差；没有把它当作物理延迟或使用该值完成新一轮真琴验收。

最终正式应用已重新打开原来的 `Real-Guitar-Practice.guitarget`，恢复“曲谱跟练”弹窗、按节拍模式、旋律声部和 **4 ms** 字段。采集仍为关闭状态，开始按钮禁用；这个字段仍是当前弹窗状态，没有宣称已持久保存校准或完成复测。
